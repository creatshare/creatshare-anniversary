# creatshare笔试题题解

 ## 基础部分：

1.char *s = ”aaa” 和char s[]=”aaa“的区别在于，

前者的s是指针变量，s存的是字符串“aaa”的地址，

后者的s是数组的首地址，存的是字符型数组的首地址；

2.面向对象：

一组数据和处理这组数据的方法可以组成对象，具有相同行为的一组对象可以组成类。面向对象的特点是封装，个体之间独立，不需要具体知道工作原理，只需要熟悉接口即可。

继承和多态：继承是个体的拓展，多态是个体在不同环境下的处理方式。

3.TCP的三次握手是在建立连接前，需要三次握手才能建立连接，第一次握手时客户端给服务器发送SYN报文，服务器接收到SYN报文，并将SYN和ACK报文返回给客户端，这就是第二次握手，客户端接受到返回的SYN和ACK报文后，再次给服务器发送ACK报文，这就是第三次握手；其中ACK报文是用来应答的，SYN报文是用来同步的。

四次挥手是在关闭连接时进行的，中断连接端可以是客户端，也可以是服务器，若是客户端发起中断，则客户端给服务器发送一个FIN报文，然后进入FIN-WAIT状态，等服务器将数据传输完成后，服务器会给客户端发送ACK和FIN报文，并且进入CLOSE_WAIT状态，客户端接受之后再给服务器发送ACK报文，这就是四次挥手，经过四次挥手，客户端知道服务器已经将数据传输完成，可以关闭连接了。

4.OSI 七层协议模型是理论模型，TCP/IP已被广泛使用成为网络互联事实上的标准，但两者之间都可认为是下层为上层提供传输服务，不关心上层传输的内容，而上层也不关心下层传输的手段，只期望能将信息最终发到通信对端的相应层次上。

5. 64位机下的int和short int 与32位机所占字节数是同样的，

故该结构体所占的内存为13字节；

6.有问题，第二段语句应改为x = scanf(“%d”,&a);

scanf的返回值是输入的字符数；

printf的返回值是打印输出的字符数；

printf("%d,%d,%d,%d\n", 2017, a, x, printf("%d\n", b));

上述语句应输出:

2017,输入的a的值，scanf(“%d”,&a)的返回值，printf("%d\n", b)的返回值；

7.*(&a[0][0]+5);

*(*(a+1)+1);

*(a[1]+1);

以上三种都可以表示a[1][1];

8.输出结果为2,4;

9.插入，冒泡，归并，希尔，快排，桶排

10.` char *link(char *a, char *b)

{

    char *c = (char *) malloc(strlen(a) + strlen(b) + 1);

    if (c == NULL) exit (1);

    char *tempc = c;

    while (*a != '\0')

    {

        *c++ = *a++;

    }

    while ((*c++ = *b++) != '\0')

    {

        *c++ = *a++;

    }

    return tempc;

} 

`

## 后端：

1.在session 中存放一个特殊标志，当表单页面被请求时，生成一个特殊的字符标志串，存在session 中，同时放在表单的隐藏域里。接受处理表单数据时，检查标识字串是否存在，并立即从 session 中删除它，然后正常处理数据。如果发现表单提交里没有有效的标志串，这说明表单已经被提交过了，忽略这次提交。使用Cookie处理，使用Cookie记录表单提交的状态，根据其状态可以检查是否已经提交表单。

2. 在linux中每个文件都属于一个用户和一个组。Linux 权限模型通过允许给每个文件系统对象设置三种独立的权限级别来工作，它们为文件的所有者、文件的组以及所有其他用户。

3. 当一个程序运行时，至少会存在一个进程，进程是系统进行资源分配和调度的基本单位，进程有独立的地址空间，而线程没有单独的地址空间，多线程共享一段地址资源，但是多线程因为是共享同一段资源，所以在切换线程时所消耗的空间与时间远远小于切换进程所需的空间与时间。

4.ssh username@host

scp /path/filename username@host:/path/

5.对象关系映射，它的作用是在关系型数据库和业务实体对象之间作一个映射，，我们在具体的操作业务对象的时候，就不需要再去和复杂的SQL语句打交道， 只需简单的操作对象的属性和方法。这样就很大程度的简化了我们的工作，使操作黑箱化，技术门槛更低。

6. 转发是服务器行为，重定向是客户端行为。HTTP中转服务器接收到请求后转发到目标服务器；客户端在接收到 3XX响应和location后对新的URI发起请求。

*其它的题我暂时还不会做。。第十题正在研究中。T_T服务器一直未响应我的请求*
